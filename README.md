# GIP Federation

GIP Federation is the federation layer for the GIP Framework. This module implements a lightweight WebSocket-based federation node for discovery and message relay between GIP instances.

Quickstart

```powershell
cd gip-federation
npm install
npm run dev
```

Or with Docker:

```powershell
docker compose up -d
```
